function computePower(num, exponent) {
      var result = 1;
      for (i = 0; i < exponent; i++) {
      result *= num;
      }
      return result;
  } 
for(let i=1 ; i<=10 ; i++)
    console.log(indx +" X " +i + " = "+ indx*i)